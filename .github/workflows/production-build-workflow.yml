name: Production build pipeline

on:
  push:
    branches:
    - master

jobs:
  docker:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Run Unit Tests
        run: docker-compose run web test


# todo: add container building and push to aws lambda